# Neural-Network-To-Recognize-Handwritten-digits
This project shows how a neural network can be built from scratch to recognize handwritten digits.  It learns by looking at pixel values in images and finding patterns that match each number, improving its accuracy over time by learning from mistakes.
